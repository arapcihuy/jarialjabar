// Note: Since we are using PHP for login (admin/login.php), this Firebase JS logic is currently unused.
// Keeping it commented out or disabled to prevent interference with the PHP form submission.

// import { initializeApp } from ...
// ...

// If you ever need to switch back to Firebase, uncomment the relevant parts.

document.addEventListener('DOMContentLoaded', () => {
    // No-op to prevent errors if this script is still included
    console.log('Admin JS loaded (Firebase Auth disabled in favor of PHP Login)');
});
